# Чек-лист для деплоя на Railway

## ✅ Перед деплоем:

- [ ] Код закоммичен и запушен в Git
- [ ] Репозиторий подключен в Railway
- [ ] Все файлы проекта на месте

## ✅ Настройки в Railway:

### 1. Settings → Source
- [ ] Правильный репозиторий подключен
- [ ] Правильная ветка (`main` или `master`)
- [ ] **Root Directory**: 
  - Если проект в корне репозитория → оставьте пустым
  - Если проект в папке `hyperion-site` → укажите `hyperion-site`

### 2. Settings → Deploy
- [ ] **Build Command**: `npm run build`
- [ ] **Start Command**: `npm start`
- [ ] **Healthcheck Path**: `/` (опционально)

### 3. Settings → Variables
Добавьте все переменные:
- [ ] `ADMIN_USERNAME=admin`
- [ ] `ADMIN_PASSWORD=ваш_надежный_пароль`
- [ ] `SESSION_SECRET=случайная_длинная_строка_минимум_32_символа`
- [ ] `NODE_ENV=production`

## ✅ Проверка деплоя:

1. [ ] Откройте вкладку **"Deployments"**
2. [ ] Проверьте статус последнего деплоя:
   - ✅ **Success** - все хорошо
   - ❌ **Failed** - смотрите логи
3. [ ] Если Failed → откройте **"View Logs"** и найдите ошибку

## ✅ Проверка работы приложения:

1. [ ] Откройте URL приложения (Railway предоставит его)
2. [ ] Проверьте главную страницу `/`
3. [ ] Проверьте страницу входа `/login`
4. [ ] Попробуйте войти с учетными данными

## 🔍 Если что-то не работает:

### Проверьте логи:
1. Railway → Deployments → View Logs
2. Ищите ошибки (красным цветом)

### Частые проблемы:

**Проблема**: "Cannot find module 'next'"
- **Решение**: Проверьте, что `package.json` содержит `next` в dependencies

**Проблема**: "Build failed"
- **Решение**: Проверьте логи сборки, возможно ошибка TypeScript

**Проблема**: "Application failed to respond"
- **Решение**: Проверьте переменные окружения, особенно `NODE_ENV=production`

**Проблема**: "Port 3000 already in use"
- **Решение**: Не указывайте PORT вручную, Railway установит его автоматически

## 📝 Важно:

- Railway автоматически устанавливает переменную `PORT`
- Не указывайте `PORT` в переменных окружения вручную
- Если проект в подпапке, укажите **Root Directory** в настройках

