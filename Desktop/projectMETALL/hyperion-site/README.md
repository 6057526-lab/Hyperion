# Hyperion CMS

Полнофункциональная система управления контентом с авторизацией, редактированием, категориями и поиском.

## Установка

```bash
npm install
```

## Настройка

Создайте файл `.env.local` в корне проекта (скопируйте из `.env.example`):

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
SESSION_SECRET=your-secret-key-change-in-production
NODE_ENV=development
```

**Важно:** Измените пароль и секретный ключ перед использованием в продакшене!

## Запуск

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Использование

1. **Главная страница** (`/`) - отображает весь загруженный контент с поиском и фильтрацией по категориям
2. **Страница входа** (`/login`) - авторизация в админ-панель
   - По умолчанию: логин `admin`, пароль `admin123`
3. **Админ-панель** (`/admin`) - полное управление контентом:
   - Добавление нового контента
   - Редактирование существующего контента
   - Удаление контента
   - Управление категориями

## Функции

- ✅ **Авторизация** - защищенная админ-панель с сессиями
- ✅ **Загрузка изображений** - поддержка любых форматов изображений
- ✅ **Текстовый контент** - заголовки и описания
- ✅ **Категории** - организация контента по категориям
- ✅ **Редактирование** - изменение существующего контента
- ✅ **Поиск** - поиск по заголовкам, тексту и категориям
- ✅ **Фильтрация** - фильтрация контента по категориям
- ✅ **Удаление** - удаление контента с подтверждением

## Структура данных

- Контент хранится в `data/content.json`
- Изображения сохраняются в `public/uploads/`
- Сессии управляются через cookies

## Безопасность

- Админ-панель защищена middleware
- Пароли хешируются с использованием SHA-256
- Сессии хранятся в httpOnly cookies
- В продакшене используйте переменные окружения для секретов

## Деплой на Railway

Проект готов для деплоя на Railway через Git.

### Шаги для деплоя:

1. **Подключите репозиторий в Railway:**
   - Зайдите на [railway.app](https://railway.app)
   - Создайте новый проект
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

2. **Настройте переменные окружения в Railway:**
   В настройках проекта (Variables) добавьте:
   ```
   ADMIN_USERNAME=admin
   ADMIN_PASSWORD=ваш_надежный_пароль
   SESSION_SECRET=случайная_длинная_строка_для_безопасности
   NODE_ENV=production
   PORT=3000
   ```

3. **Railway автоматически:**
   - Определит Next.js проект
   - Установит зависимости (`npm install`)
   - Соберет проект (`npm run build`)
   - Запустит сервер (`npm start`)

4. **После деплоя:**
   - Railway предоставит URL вашего приложения
   - Откройте `/login` для входа в админ-панель
   - Используйте учетные данные из переменных окружения

### Важные замечания для Railway:

- ⚠️ **Хранение данных**: Railway использует ephemeral storage. Данные (`data/content.json` и `public/uploads/`) будут удаляться при перезапуске. Для постоянного хранения рассмотрите:
  - Railway Volumes (платная функция)
  - Внешнее хранилище (S3, Cloudinary для изображений)
  - База данных (PostgreSQL, MongoDB)

- 🔒 **Безопасность**: Обязательно измените `ADMIN_PASSWORD` и `SESSION_SECRET` на надежные значения!

- 🌐 **Домен**: Railway автоматически предоставляет домен, но вы можете подключить свой кастомный домен в настройках.

